# iteration-19-manual-test.md · 手动测试清单（用户执行）

> 主控列步骤，用户在浏览器 / 微信开发者工具实际操作并回填。
> 遵循 [`.agents/testing/SKILL.md`](../../.agents/testing/SKILL.md) §不能做（手动测试）边界。

## 前置条件
- 自动测试 [iteration-19-auto-test.md](iteration-19-auto-test.md) 已 PASS（16/16 ✅ + 3 bug 修复）
- Vue dev server 跑（端口 5173）
- 微信开发者工具打开 `apps/shop-miniprogram`
- 浏览器以以下任一账号登录后台测试：
  - `admin / admin123`（super_admin，可见全部菜单）
  - `sales / sales123`（sales_ops，可见 PIM/OMS/营销，无系统管理）
  - `warehouse / wh123`（warehouse，只能见 WMS，不可见营销）

## 测试用例（共 12 项，仅 UI 交互不能 curl）

### A · Vue 后台 — 营销 / 优惠券

| # | 步骤 | 期望 | 实际 | PASS |
|---|---|---|---|---|
| M1 | 用 admin 登录 → 看左侧菜单 | 出现"营销"父级，含"优惠券"子菜单 | 实测填写 | ☐ |
| M2 | 用 sales 登录 → 看左侧菜单 | 出现"营销"，但"系统管理"隐藏 | 实测填写 | ☐ |
| M3 | 用 warehouse 登录 → 看左侧菜单 | **不**出现"营销" | 实测填写 | ☐ |
| M4 | admin 进入 `/marketing/coupons` | 列表显示 seed 的 2 张 + 自动测试创建的，"规则"列正确显示如"满 ¥99.00 减 ¥10.00" | 实测填写 | ☐ |
| M5 | 点"新增优惠券" → 类型=满减 → 减免=15 → 门槛=200 → 有效期默认 → 提交 | toast 已新增；列表多 1 行 | 实测填写 | ☐ |
| M6 | 同上但类型=折扣 → 百分比=20 → 封顶=50 → 门槛=100 → 提交 | 列表多 1 行，"规则"列显示"满 ¥100.00 减 20%（最多 ¥50.00）" | 实测填写 | ☐ |
| M7 | 选一张 active 券点"编辑" → 改名 → 提交 | 列表名称已变；类型/折扣值灰禁不可改 | 实测填写 | ☐ |
| M8 | 选一张 active 券点"停用" → 弹框确认 → 是 | 行变为停用 tag；操作列"停用"按钮消失 | 实测填写 | ☐ |

### B · 小程序 — 领券中心 + 我的优惠券

| # | 步骤 | 期望 | 实际 | PASS |
|---|---|---|---|---|
| M9 | 我的页 → 点"领券中心" | 列表显示 active 券，已领的标灰显示"已领过" | 实测填写 | ☐ |
| M10 | 点未领的券"领取"按钮 | toast 领取成功；按钮变"已领过" | 实测填写 | ☐ |
| M11 | 我的页 → 点"我的优惠券" → 切 tab "未使用 / 已使用 / 已过期" | 各 tab 数据正确，已用的下方显示"已用于 SO..." | 实测填写 | ☐ |

### C · 小程序 — 结算页核销

| # | 步骤 | 期望 | 实际 | PASS |
|---|---|---|---|---|
| M12 | 购物车选一件 ≥¥99 商品 → 进入结算 → 点"选择优惠券" | 跳到我的优惠券（选择模式），未达门槛的标灰，可用的有"选用"标记 | 实测填写 | ☐ |
| M13 | 点击一张可用券 → 自动返回结算页 | 优惠券行显示"满 99 减 10 -¥10.00"；合计区出现"优惠 -¥10.00"；应付重算 | 实测填写 | ☐ |
| M14 | 提交订单 → 进入支付页 → 返回订单详情查看 | order.discount = 1000；total = goods + 1000 - 1000 = goods（freight 10 抵 discount 10） | 实测填写 | ☐ |

## 用户填写指南

每行 `实际` 简短描述，`PASS` 勾 ✅ 或 ❌。

## 测试时间
（用户填）：_________________________
