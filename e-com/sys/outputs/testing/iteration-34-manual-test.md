# iteration-34-manual-test.md · 手动测试清单（用户执行）

> 主控列步骤，用户在 Vue 后台 + 小程序实际操作并回填。

## 前置条件
- auto-test [iteration-34-auto-test.md](iteration-34-auto-test.md) 已 PASS（14/14 ✅）
- Vue dev server 跑（vite 热更已接管）
- OMS 子菜单加 1 项：**换货审批**（super_admin + sales_ops 可见）
- 小程序 me 页"订单"分组加"我的换货"；订单详情页 shipped/completed 状态加"申请换货"按钮

## 测试用例（共 8 项）

### A · 小程序：用户申请

| # | 步骤 | 期望 | 实际 | PASS |
|---|---|---|---|---|
| M1 | 用户登录 → 我的 → 全部订单 → 进入一个 shipped/completed 订单详情 → 底部点"申请换货" | 跳转申请页 + 加载订单项 + 显示原 SKU + 数量 + 单价 | 实测填写 | ☐ |
| M2 | 勾选一行 → 展开数量/换成 SKU 输入框 → 数量 1 → 新 SKU 填 SPU001-002 → 原因写"尺码不对" → 选 1 张凭证图 → 提交 | toast "已提交 EX..."，跳转"我的换货" | 实测填写 | ☐ |
| M3 | 我的换货页 → 看新单 | 显示状态"待审批"+ 订单号 + 原因 + 取消按钮 | 实测填写 | ☐ |
| M4 | 同一订单项再点申请换货 → 再选同行 → 提交 | 红色 toast"已有进行中的换货单" | 实测填写 | ☐ |

### B · Vue admin：审批 + 流转

| # | 步骤 | 期望 | 实际 | PASS |
|---|---|---|---|---|
| M5 | admin/admin123 → OMS / 换货审批 → 看到 M2 留下的单 | 状态 tag = "待审批"（warning 黄色），右侧"通过/拒绝"按钮 | 实测填写 | ☐ |
| M6 | 点"通过" → 状态变 approved（蓝 "已通过 · 等寄回"）→ "收到旧货"按钮显示 → 点 → 弹两步输入（物流单号 + 备注） | 状态变 received_old + 详情对话框能看见时间戳和单号 | 实测填写 | ☐ |
| M7 | 继续点"发新货" → 填新单号 → 状态 sent_new → 再点"完成" | 状态变 completed（绿）+ 详情显示完整时间线 | 实测填写 | ☐ |
| M8 | sales/sales123 登录看换货审批菜单 → warehouse 登录 → 不可见换货审批菜单 | sales 可见可操作；warehouse 完全无此菜单 | 实测填写 | ☐ |

## 用户填写指南
每行 `实际` 栏简短描述（"申请提交对 / 阻断对 / 审批流转完整 / RBAC 对"等），`PASS` 栏勾 ✅ 或 ❌。

## 测试时间
（用户填）：_________________________
