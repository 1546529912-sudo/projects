# Sample Pencil File

This folder contains a complete sample generated by the skill:

- `pencil-new.pen`
- `images/`

Keep the `.pen` file and the `images/` folder together. The Pencil file references local image assets, so moving only the `.pen` file may cause missing images when opened.
