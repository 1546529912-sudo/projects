# T-3 建议稿：「高级字段」进度条目标题与验收口径

> **性质：** 供 **产品 Agent** 改写、**Owner** 定案；**非**已生效需求变更，直至 Owner 写入 `progress.md` 或口头确认。

## 1. 问题回顾

`progress.md` 中某条 **「高级字段 / 文件图片 / 地图」** 类条目同时存在：

- 勾选或意图为 **「开发完成」**；
- 括号或备注写 **依赖对象存储、暂不实现**。

测试侧无法判断：该条勾「测试通过」时，是否要验收文件/图片/地图字段，还是仅验收已落地类型。

## 2. 建议定案方向（推荐）

**拆成两条（或子勾选），不搞一条双解：**

| 子项 | 范围 | 开发状态（示例） | 测试验收 |
|------|------|------------------|----------|
| **A. 高级字段（结构化）** | 文本、数字、日期、单选/多选、布尔、URL、自动编号等 **已接入** 的类型 | 与当前实现一致 | 按 §3.8 / 设置 / 表单侧 **可测** 项验收 |
| **B. 附件型 / 地图型** | 文件、图片、地图坐标、重度依赖 **外部存储或地图 SDK** 的能力 | **明确列为未开发或 P2+** | **不纳入** 当前阶段「测试通过」必要条件；恢复开发时再单列进度项 |

**`progress.md` 标题改写示例（供粘贴）：**

- 将原一条改为：  
  - **`[x]` 高级自定义字段（结构化类型 + 表单/字典/必填规则）**  
  - **`[ ]` 高级字段：文件 / 图片 / 地图（依赖对象存储与第三方，另行立项）**

## 3. 若 Owner 坚持合并为一条

若必须保留单一条目，则需在条目中 **唯一口径**：

> 「测试通过」仅表示 **已实现字段类型** 与 **当前数据模型**；**不包含** 文件/图片/地图字段的端到端验收。

并在「当前问题」或脚注中 **冻结**未实现子能力，避免与 `.claude/docs/` 全文逐项对打。

## 4. 测试 Agent 执行含义

定案后，`crm-regression-test-report.md` 中 **§6.1** 类歧义可关闭；T-4 批量勾选时对本条 **不再双解**。

---

*本文件由自动化/Owner 工作流产出，最终以上述角色修订为准。*
